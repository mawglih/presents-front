import React from 'react';
import styles from './addpresents.css';

const AddPresents = () => (
  <div className={styles.container}>
    <h1>Add presents</h1>
  </div>
);

export default AddPresents;
